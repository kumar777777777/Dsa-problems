class Solution(object):
    def romanToInt(self, s):
        val = {
            "I": 1, "V": 5, "X": 10,
            "L": 50, "C": 100,
            "D": 500, "M": 1000
        }

        i = 0
        total = 0

        while i < len(s):
            if i <= len(s) - 2:
                if val[s[i]] >= val[s[i + 1]]:
                    total += val[s[i]]
                else:
                    total += val[s[i + 1]] - val[s[i]]
                    i += 1
            else:
                total += val[s[i]]

            i += 1

        return total